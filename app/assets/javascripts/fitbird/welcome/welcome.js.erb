module.config(['$stateProvider',
  function($stateProvider) {
    $stateProvider
      .state('welcome', {
        url: '/',
        templateUrl: '<%= asset_path("welcome/welcome.html.haml") %>',
        controller: 'WelcomeCtrl'
      });
}]);
