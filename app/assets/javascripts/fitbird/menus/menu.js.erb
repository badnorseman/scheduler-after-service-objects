module.directive('menu', function() {
  return {
    replace: true,
    restrict: 'E',
    transclude: true,
    scope: {
      signedIn: '=signedIn'
    },
    templateUrl: '<%= asset_path("menus/menu.html.haml") %>',
    controller: 'MenuCtrl'
  };
});
